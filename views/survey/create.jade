extends ../layout

block content
  .container
    h1.page-header 설문조사 만들기
    section
      div
        label(for="newName") 제목
        input(type="text" id="newName" name="newName" placeholder="설문조사 이름을 지정합니다.")
      span "최대 250자까지 입력할 수 있습니다."
      div
        a.btn.btn-default(href='/survey/form') 진행!
        
      a(href='/form') To form
